#include "monty.h"

/**
 * f_stack - sets the data structure to stack mode (LIFO)
 * @head: stack head
 * @counter: line_number (unused)
 * Return: no return
 */
void f_stack(stack_t **head, __attribute__((unused)) unsigned int counter)
{
    bus.lifi = 0;
}

